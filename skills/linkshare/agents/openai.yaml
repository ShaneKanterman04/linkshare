interface:
  display_name: "Linkshare"
  short_description: "Share and consume one-time links"
  default_prompt: "Use $linkshare to share a link with me or consume links waiting for agents."

policy:
  allow_implicit_invocation: true
